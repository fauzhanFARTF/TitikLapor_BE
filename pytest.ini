[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = test_*.py
addopts = -ra --strict-markers
testpaths = tests
markers =
    unit: pengujian unit tanpa akses database
    integration: pengujian integrasi (butuh database PostGIS)
